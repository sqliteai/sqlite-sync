# Minimal big-endian toolchain for `make unittest-s390x`. Runs under QEMU emulation
# (Docker Desktop, or docker/setup-qemu-action on CI); the sources are mounted, not copied.
FROM alpine:3.20
RUN apk add --no-cache build-base
